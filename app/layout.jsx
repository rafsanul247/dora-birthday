import "./globals.css";

export const metadata = {
  title: "Happy Birthday Doraemon 💙",
  description: "A tiny birthday universe for Doraemon — September 3.",
};

export default function RootLayout({ children }) {
  
  return <html lang="en"><body>{children}</body></html>;
}
